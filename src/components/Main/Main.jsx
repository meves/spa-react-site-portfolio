import style from './Main.module.scss';

const Main = (props) => {
    return (
        <section className={style.sectionMain}>            
            Main
        </section>
    );
};

export default Main;
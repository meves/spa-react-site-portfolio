import style from './Forum.module.scss';

const Forum = (props) => {
    return (
        <section className={style.sectionForum}>
            Forum
        </section>
    );
};

export default Forum;
const initialState = {
    message: 'Forum'
};

const forumReducer = (state=initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default forumReducer;

// profilePage
export const receiveMessage = state => state.profilePage.message;
export const receiveProfile = state => state.profilePage.profile;
export const receiveStatus = state => state.profilePage.status;

// posts
export const receiveTitle = state => state.profilePage.myPost.title;
export const receivePosts = state => state.profilePage.myPost.posts;

